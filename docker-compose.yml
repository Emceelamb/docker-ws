version: "3.1"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 8000:8000
  lt:
    build:
      context: .
      dockerfile: Dockerfile-lt
    links:
      - web
    command: --local-host web --port 8000
